# Example .env file for React applications using KeyRetriever functions
# Copy this to your React app's .env file and update the endpoints

# For OpenAI configuration retrieval (MortonGroupAICred function app)
# Used by: examples/reactClient.tsx, examples/reactClientProxy.tsx
REACT_APP_AZURE_FUNCTION_URL=https://mortongroupaicred-hugxh8drhqabbphb.canadacentral-01.azurewebsites.net

# For blob storage operations (StorageProxy function app)  
# Used by: examples/reactClientBlob.tsx
# Note: The blob client automatically appends /api/blob to this URL
REACT_APP_AZURE_FUNCTION_URL=https://storageproxy-c6g8bvbcdqc7duam.canadacentral-01.azurewebsites.net

# Important: You can only use one REACT_APP_AZURE_FUNCTION_URL at a time
# Choose the appropriate endpoint based on which functionality you need:
# - Use MortonGroupAICred endpoint for OpenAI features
# - Use StorageProxy endpoint for blob storage features
# - Or deploy with different environment variable names if you need both